# Built-in Functions -Binary Conversion Using Built-in Functions in Python

## 🎯 Aim
To write a Python program to convert the number **16** into its **binary representation** using built-in Python functions.

## 🧠 Algorithm
1. Assign the value `16` to a variable `a`.
2. Use the built-in `bin()` function to convert the number to binary.
3. Print the result.

## 🧾 Program

```
a=int(input())
x=bin(a)
print(x)
```

## Output
<img width="1081" height="362" alt="image" src="https://github.com/user-attachments/assets/dc8535f6-6b7c-40fa-8161-6c1c0ecae620" />

## Result
Thus to write a python program to convert the decimal number into its binary representation using built-in Python functions is created and verified successfully.
